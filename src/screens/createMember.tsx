import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const createMember = () => {
  return (
    <View>
      <Text>createMember</Text>
    </View>
  )
}

export default createMember

const styles = StyleSheet.create({})